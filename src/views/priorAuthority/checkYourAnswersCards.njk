{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}
{% macro justificationCard(justification, basePath, title = "Justification") %}
  {% set justificationValueHtml %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-three-quarters-from-desktop">
      <p class="govuk-body govuk-!-margin-bottom-0">{{ (justification or "Not provided") | escape }}</p>
    </div>
  </div>
  {% endset %}
  {{ govukSummaryList({
    classes: "pa-justification-summary-list",
    card: {
      title: { text: title },
      actions: {
        items: [{
          href: basePath ~ "/justification",
          text: "Change",
          classes: "govuk-link--no-visited-state",
          visuallyHiddenText: "justification"
        }]
      }
    },
    rows: [{
      value: { html: justificationValueHtml }
    }]
  }) }}
{% endmacro %}
{% macro supportingDocumentsCard(uploadedDocuments, basePath, buildSupportingDocumentsRows) %}
  {% set documentRows = buildSupportingDocumentsRows(uploadedDocuments, basePath) %}
  {{ govukSummaryList({
    card: {
      title: { text: "Supporting files" },
      actions: {
        items: [{
          href: basePath ~ "/document-upload",
          text: "Change",
          classes: "govuk-link--no-visited-state",
          visuallyHiddenText: "supporting files"
        }]
      }
    },
    rows: documentRows if documentRows.length else [{ value: { text: "Not provided" } }]
  }) }}
{% endmacro %}