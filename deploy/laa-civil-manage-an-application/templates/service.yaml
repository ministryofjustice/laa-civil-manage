apiVersion: v1
kind: Service
metadata:
  name: {{ include "laa-civil-manage-an-application.fullname" . }}
  labels:
    {{- include "laa-civil-manage-an-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laa-civil-manage-an-application.selectorLabels" . | nindent 4 }}
